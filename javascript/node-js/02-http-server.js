const http=require('node:http');const server=http.createServer((request,response)=>{if(request.url==='/json'){response.writeHead(200,{'Content-Type':'application/json'});response.end(JSON.stringify({message:'Hello from Node.js'}));return}response.writeHead(200,{'Content-Type':'text/html'});response.end('<h1>Node HTTP Server</h1><p>Request received and response sent.</p>')});server.listen(3000,()=>console.log('Server running at http://localhost:3000'));
