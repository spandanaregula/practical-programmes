const express=require('express');const app=express();app.use(express.json());const products=[{id:1,name:'Notebook',price:8}];app.get('/api/products',(req,res)=>res.status(200).json(products));app.post('/api/products',(req,res)=>res.status(201).json({...req.body,id:Date.now()}));app.patch('/api/products/:id',(req,res)=>res.status(200).json({id:req.params.id,...req.body}));app.delete('/api/products/:id',(req,res)=>res.sendStatus(204));app.use((req,res)=>res.status(404).json({error:'Route not found'}));app.listen(3005,()=>console.log('REST API: http://localhost:3005/api/products'));
