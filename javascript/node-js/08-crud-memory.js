const express=require('express');const app=express();app.use(express.json());let students=[{id:1,name:'Asha',course:'Web'}];app.get('/students',(req,res)=>res.json(students));app.post('/students',(req,res)=>{const student={id:Date.now(),...req.body};if(!student.name)return res.status(400).json({error:'name required'});students.push(student);res.status(201).json(student)});app.put('/students/:id',(req,res)=>{const index=students.findIndex(s=>s.id==req.params.id);if(index<0)return res.sendStatus(404);students[index]={...students[index],...req.body};res.json(students[index])});app.delete('/students/:id',(req,res)=>{students=students.filter(s=>s.id!=req.params.id);res.sendStatus(204)});app.listen(3003,()=>console.log('CRUD API: http://localhost:3003/students'));
