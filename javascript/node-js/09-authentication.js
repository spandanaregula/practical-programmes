const express=require('express');const bcrypt=require('bcryptjs');const jwt=require('jsonwebtoken');const app=express();app.use(express.json());const users=[];const secret='development-secret';app.post('/register',async(req,res)=>{const password=await bcrypt.hash(req.body.password,10);users.push({email:req.body.email,password});res.status(201).send('Registered')});app.post('/login',async(req,res)=>{const user=users.find(item=>item.email===req.body.email);if(!user||!(await bcrypt.compare(req.body.password,user.password)))return res.sendStatus(401);res.json({token:jwt.sign({email:user.email},secret,{expiresIn:'1h'})})});function auth(req,res,next){try{req.user=jwt.verify(req.headers.authorization?.replace('Bearer ','')||'',secret);next()}catch{res.sendStatus(401)}}app.get('/protected',auth,(req,res)=>res.json({message:'Protected data',user:req.user}));app.listen(3004,()=>console.log('Auth API: http://localhost:3004'));
