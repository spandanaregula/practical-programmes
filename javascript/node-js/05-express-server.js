const express=require('express');const app=express();app.use(express.json());app.use(express.static('public'));app.get('/',(request,response)=>response.send('<h1>Express server</h1>'));app.post('/message',(request,response)=>response.status(201).json({received:request.body}));app.use((request,response)=>response.status(404).json({error:'Not found'}));app.listen(3000,()=>console.log('Express server: http://localhost:3000'));
